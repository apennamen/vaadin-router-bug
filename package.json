{
  "scripts": {
    "start:build": "es-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start": "es-dev-server --app-index index.html --node-resolve --open --watch"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^0.21.0",
    "es-dev-server": "^1.5.0",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.4"
  },
  "name": "wired-calendar-bug",
  "version": "0.0.0",
  "description": "Webcomponent wired-calendar-bug following open-wc recommendations",
  "author": "wired-calendar-bug",
  "license": "MIT",
  "dependencies": {
    "@vaadin/router": "^1.7.1",
    "lit-element": "^2.0.1",
    "lit-html": "^1.0.0"
  }
}
